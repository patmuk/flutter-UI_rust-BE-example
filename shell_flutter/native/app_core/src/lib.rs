// mod crux;
pub mod todo_list;

// start streaming the view model.
// sending the view model on request (as an effect) would be better,
// but this is to demonstrate streaming.
// tokio::spawn(crux::stream_view());
